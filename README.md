# Sitemap Fixer

A local tool to crawl AI-generated websites, clean bad URLs, and generate a valid XML sitemap.

## Usage
python main.py

## Features
- Crawl website
- Remove invalid URLs
- Normalize links
- Generate sitemap.xml
